/**
 * Write a function that returns the first element of an input array. For
 * example:
 */

first(['Earth', 'Moon', 'Mars']); // 'Earth'

function first(array) {
  console.log(array[0]);
  return array[0];
}

/**
 * Bonus question: what would you return if the array was empty?
 * 
 * Answer: undefined
 */