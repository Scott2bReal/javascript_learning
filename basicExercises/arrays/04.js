// Split the string alphabet into an array of characters

let alphabet = 'abcdefghijklmnopqrstuvwxyz';

const letters = alphabet.split('');

// Could also do:

const anotherLetters = Array.from(alphabet);