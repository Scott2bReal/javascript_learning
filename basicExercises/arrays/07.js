/**
 * Predict the output of the below code. When you run the code, do you see what
 * you expected? Why or why not?
 */

let array1 = [2, 6, 4]; let array2 = [2, 6, 4];

console.log(array1 === array2);

/**
 * false will be logged to the console. The arrays contain identical elements,
 * but are actually two different arrays stored at differenct locations in
 * memory.
 */