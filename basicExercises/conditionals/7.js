// Predict the output of the following code:

if (false || true) {
      console.log('Yes!');
} else {
      console.log('No...');
}

/**
 * 'Yes!' will be logged to the console because only one side of the the || statement needs to be truthy
 * for the statement to be evaluated as true.
 */
