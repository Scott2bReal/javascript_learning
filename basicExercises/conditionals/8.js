// Predict the output of the following code:

if (true && false) {
      console.log('Yes!');
} else {
      console.log('No...');
}

/**
 * 'No...' will always be logged to the console because BOTH sides of the && statement must be true
 * for the whole statement to be evaluated as true
 */
